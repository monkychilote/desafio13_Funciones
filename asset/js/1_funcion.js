/**Esta es la respuesta a transformar esta declaracion de funcion.

function example(a, b, c){
return a+b+c
}

a una funcion de expresion
*/
const example = function (a, b, c) {
  return a + b + c;
};
